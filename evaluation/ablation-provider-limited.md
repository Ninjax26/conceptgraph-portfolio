# Ablation blocked

Neo4j connectivity failed with `ServiceUnavailable`. No retrieval scores were collected.
The committed `evaluation/ablation-reference.md` remains the last successful isolated run.
